# Local dev: Vite proxies /api → localhost:5000.
VITE_API_BASE_URL=/api
# Production (Vercel): set to Render API if /api rewrite is unavailable:
# VITE_API_BASE_URL=https://the-exchange-tunnel.onrender.com/api
# Username of the Telegram bot (no @) — for https://core.telegram.org/widgets/login
VITE_TELEGRAM_BOT_NAME=
